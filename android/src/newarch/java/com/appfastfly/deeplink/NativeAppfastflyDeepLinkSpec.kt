package com.appfastfly.deeplink

import com.facebook.react.bridge.ReactApplicationContext

/**
 * New Architecture spec stub.
 * On New Architecture builds, Codegen generates the real spec at:
 *   build/generated/source/codegen/java/com/appfastfly/deeplink/NativeAppfastflyDeepLinkSpec.java
 * This file is only referenced via sourceSets when newArchEnabled=true so that
 * the module compiles correctly before Codegen runs for the first time.
 */
abstract class NativeAppfastflyDeepLinkSpec(reactContext: ReactApplicationContext) :
  com.facebook.react.bridge.ReactContextBaseJavaModule(reactContext)
